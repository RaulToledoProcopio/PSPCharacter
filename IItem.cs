namespace Ejercicio1;

// Interfaz para aplciar los ítems del inventario

public interface IItem { 
    void Apply(Character character); // Lo único que hacemos en este interfaz es crear un método que aplicará los efectos del ítem sobre el personaje.
}